# experiments/.env — copy this to experiments/.env (gitignored) and fill in.
# run-agent.sh auto-loads experiments/.env if present; the CLI -p/-m flags override.

# pi provider name, as defined in ~/.pi/agent/models.json:
PI_PROVIDER=my-provider

# model id the provider serves:
PI_MODEL=my-model-id
